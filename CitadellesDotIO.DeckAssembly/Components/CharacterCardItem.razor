@using CitadellesDotIO.DeckAssembly.Model

<RadzenText TextStyle="TextStyle.Body1">@CharacterCard?.Character.Name</RadzenText>
@code {
    [Parameter]
    public CharacterCard? CharacterCard { get; set; }
}
